package mcjty.rftoolsbase.api.xnet.keys;

/**
 * This is a global ID representing a network. It is given by network providers.
 * In a chunk network ID's are coupled with blob ID's.
 */
public record NetworkId(int id) {
}
